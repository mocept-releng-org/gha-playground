{
  "name": "@mocept/bump-version",
  "version": "1.0.0",
  "private": true,
  "description": "Computes the next semver from a bump type and current tag",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --minify --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^6.0.0"
  }
}